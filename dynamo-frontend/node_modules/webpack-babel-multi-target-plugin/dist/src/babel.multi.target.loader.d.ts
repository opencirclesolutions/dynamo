import { Loader, NewLoader } from 'webpack';
export interface BabelMultiTargetLoaderOptions {
    isBabelMultiTargetLoader: true;
    loaderOptions?: {
        [key: string]: any;
    };
}
export declare class BabelMultiTargetLoader implements NewLoader {
    readonly loader: string;
    readonly options: BabelMultiTargetLoaderOptions;
    constructor(loader?: Loader);
}
