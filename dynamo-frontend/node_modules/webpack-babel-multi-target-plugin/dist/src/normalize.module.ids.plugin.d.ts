import { Compiler, Plugin } from 'webpack';
export declare class NormalizeModuleIdsPlugin implements Plugin {
    apply(compiler: Compiler): void;
    private pluginName;
    private applyModuleIdNormalizing;
    private applyConditionJsonpCallback;
    private applyHtmlWebpackTagOrdering;
}
