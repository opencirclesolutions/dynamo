import { TranslateEntityPipe } from './translate-entity.pipe';

describe('TranslateEntityPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateEntityPipe();
    expect(pipe).toBeTruthy();
  });
});
