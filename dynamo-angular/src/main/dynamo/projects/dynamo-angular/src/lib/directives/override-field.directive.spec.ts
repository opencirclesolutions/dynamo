import { OverrideFieldDirective } from './override-field.directive';

describe('OverrideFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new OverrideFieldDirective();
    expect(directive).toBeTruthy();
  });
});
