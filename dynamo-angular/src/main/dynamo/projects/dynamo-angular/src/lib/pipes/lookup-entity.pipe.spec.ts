import { LookupEntityPipe } from './lookup-entity.pipe';

describe('LookupEntityPipe', () => {
  it('create an instance', () => {
    const pipe = new LookupEntityPipe();
    expect(pipe).toBeTruthy();
  });
});
