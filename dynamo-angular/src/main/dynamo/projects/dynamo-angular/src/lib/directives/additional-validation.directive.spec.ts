import { AdditionalValidationDirective } from './additional-validation.directive';

describe('AdditionalValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new AdditionalValidationDirective();
    expect(directive).toBeTruthy();
  });
});
