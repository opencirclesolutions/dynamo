import { HiddenFieldDirective } from './hidden-field.directive';

describe('HiddenFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new HiddenFieldDirective();
    expect(directive).toBeTruthy();
  });
});
